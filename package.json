{
  "name": "animate-vpaid-bridge",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "http-server -p 8787",
    "lint": "standard src/**/*.js",
    "browserify": "browserify src/index.js -o dist/animate-vpaid-bridge.js",
    "browserify-kahweetest": "browserify src/examples/kahweetest.js -o examples/ad-tags/kahweetest.js",
    "browserify-motion": "browserify src/examples/motion.js -o examples/ad-tags/motion.js",
    "browserify-examples": "npm run browserify-motion && npm run browserify-kahweetest",
    "uglify": "uglifyjs dist/animate-vpaid-bridge.js -o dist/animate-vpaid-bridge.min.js -c -m --screw-ie8",
    "build": "npm run browserify && npm run uglify"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kahwee/animate-vpaid-bridge.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/kahwee/animate-vpaid-bridge/issues"
  },
  "homepage": "https://github.com/kahwee/animate-vpaid-bridge#readme",
  "dependencies": {
    "vpaid-ad": "^2.0.4"
  },
  "devDependencies": {
    "babel-core": "^6.13.2",
    "babel-preset-es2015": "^6.13.2",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "mocha": "^3.0.2",
    "standard": "^7.1.2",
    "uglify-js": "^2.7.1"
  },
  "keywords": [
    "animate",
    "animation",
    "animate cc",
    "adobe",
    "vpaid",
    "ad",
    "adtech",
    "vast",
    "debugging",
    "advertisement",
    "unit"
  ],
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ]
    ]
  }
}
